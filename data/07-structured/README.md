# 07-structured — 구조화 질문 데이터셋

`data/02-common`, `data/03-regions`, `data/04-jobs` 아래 마크다운 파일에 수집된
공무원 면접 질문을 AI 시스템에서 바로 사용할 수 있도록 구조화한 JSON 데이터셋.

- **생성 스크립트**: `scripts/build_dataset.py` (Python 표준 라이브러리만 사용)
- **재생성 방법**: 리포지토리 루트에서 `python3 scripts/build_dataset.py`
- **출력 파일**: `data/07-structured/questions.json` (UTF-8, `ensure_ascii=False`)
- 마크다운 원본은 읽기만 하며 수정하지 않는다. 원본이 갱신되면 스크립트를 다시 실행하면 된다.

## 파일 구조

```json
{
  "meta": {
    "name": "korean-civil-service-interview-questions",
    "generated_at": "…",            // 생성 시각 (UTC, ISO-8601)
    "generator": "scripts/build_dataset.py",
    "source_dirs": ["02-common", "03-regions", "04-jobs"],
    "total_questions": 1941,
    "files_scanned": [ {"file": "...", "questions": N}, … ],
    "distributions": { "category": …, "type": …, "reliability": …, "region": …, "job": …, "year": … },
    "schema": { …필드 설명… }
  },
  "questions": [ { …아래 스키마… }, … ]
}
```

## 질문 스키마 (`questions[]`)

| 필드 | 타입 | 설명 |
|---|---|---|
| `id` | string | 고유 ID. `q00001` 형식(파일 정렬 순서 기반 일련번호). 재생성 시 원본 변경에 따라 번호가 밀릴 수 있으므로 영구 키가 필요하면 `text`+`source_file` 해시를 별도로 만들 것 |
| `text` | string | 질문·발표주제 본문. 마크다운 굵게/코드 마커 제거, NFC 정규화, `— 연결 공직가치: …`·`(법률저널 보도)` 류 편집·출처 주석 꼬리 제거 |
| `category` | string | `common`(직렬·지역 공통) / `region`(지역별) / `job`(직렬별) — 원본 디렉토리 기반 |
| `region` | string \| null | 지역명. `03-regions` 파일명 기반(주요시군 파일은 `## 수원시` 등 섹션 제목의 시·군명), 그 외에는 태그(`[2023/서울/정책]` 등)에서 추출. 약칭은 공식 명칭으로 정규화(서울 → 서울특별시) |
| `job` | string \| null | 직렬명. `04-jobs` 파일명 기반(`기타직렬.md`는 섹션 제목의 직렬명), 그 외에는 태그·문두 `(세무직)` 표기에서 추출 |
| `year` | int \| null | 출제(복원) 연도. 태그(`[2021/공통/…]`), 문두 연도, `## 2025년` 류 연도 섹션 제목 순으로 추출. 미상이면 null |
| `type` | string \| null | 질문 유형: `공직가치` `인성` `직무` `지역현안` `상황형` `시사` `발표` `후속` 중 하나. 태그 → 섹션 제목 → 파일명 순으로 추론 |
| `reliability` | string \| null | 신뢰도: `확인`(출처 문서에서 직접 확인) / `복원`(응시자 기억 기반 복원, `복원참고` 포함) / `기출`(기출로 표기) / `예상`(현안 기반 예상·대비 질문) / `빈출`(빈출·단골 표기). 표기가 없으면 null |
| `source_file` | string | 리포지토리 루트 기준 원본 마크다운 경로 |

## 통계 요약 (2026-07-04 생성 기준)

- **총 질문 수: 1,941** (41개 파일에서 추출)

### 카테고리
| category | 건수 |
|---|---|
| job | 1,366 |
| common | 291 |
| region | 284 |

### 질문 유형
| type | 건수 |
|---|---|
| 직무 | 947 |
| 상황형 | 420 |
| 지역현안 | 255 |
| 공직가치 | 131 |
| 인성 | 99 |
| 시사 | 27 |
| 발표 | 22 |
| 후속 | 11 |
| (미분류) | 29 |

### 신뢰도
| reliability | 건수 |
|---|---|
| 빈출 | 356 |
| 기출 | 129 |
| 복원 | 125 |
| 예상 | 78 |
| 확인 | 37 |
| (표기 없음) | 1,216 |

### 지역 (상위)
세종특별자치시 44 · 서울특별시 43 · 경기도 31 · 충청남도 22 · 경상북도 20 ·
대구광역시 19 · 인천광역시 19 · 경상남도 16 · 전라남도 15 · 전북특별자치도 15 ·
부산광역시 13 · 제주특별자치도 13 · 충청북도 11 · 강원특별자치도 10 · 대전광역시 10 ·
울산광역시 10 · 광주광역시 9 · (경기 주요시군: 화성 5, 안양 4, 용인 3, 안산 3, 수원 2, 성남 2, 고양 2, 부천 2) ·
지역 미지정 1,598

### 직렬 (상위)
교육행정직 159 · 일반행정직 123 · 사서직 91 · 전기직 91 · 농업직 90 · 전산직 88 ·
환경직 88 · 사회복지직 83 · 간호직 83 · 보건직 82 · 세무직 82 · 기계직 58 · 토목직 56 ·
건축직 51 · 해양수산직 35 · 녹지직 34 · 방재안전직 33 · 수의직 33 · 시설관리직 26 ·
운전직 24 · 우정직 3 · 직렬 미지정 526

### 연도
2015(2) 2016(1) 2018(2) 2019(6) 2020(15) 2021(9) 2022(21) 2023(7) 2024(21) 2025(12) 2026·예상(12) · 미상 1,833

## 추출 규칙 개요

1. 불릿(`- `, `* `)으로 시작하는 줄만 후보로 본다 (체크박스 `- [ ]`, URL 포함 줄 제외).
2. 다음 중 하나면 질문으로 채택: 물음표 포함 / 명령형 어미(~보라, ~하시오 등)로 종결 /
   `질문1 / 질문2 / …` 형태의 복원 질문 목록 / `[연도/직렬/유형]` 태그 보유(해설성 문장 제외).
3. 출처 안내·면접 형식 설명·복기 메모(예: "게시글 존재, 열람 필요", "평정요소: …", "…라는 후기가 다수")는
   표지어·어미·머리말 사전으로 걸러낸다.
4. 태그를 `/` 로 분해해 연도·지역·직렬·유형·신뢰도를 파싱하고, 태그가 없으면
   섹션 제목·파일명에서 유형/직렬/지역을 보충한다.

## 알려진 한계

- **미분류 필드**: 원본에 태그가 없는 질문(주로 04-jobs 직무 질문)은 `year`(94% 미상)와
  `reliability`(63% 없음)가 null이다. 04-jobs 질문은 특정 연도가 아닌 "직렬 지식 체크리스트" 성격이므로 정상.
- **복합 질문 미분리**: "본인의 장점과 단점은? 그 이유는?"처럼 한 불릿에 2개 이상의 질문이 있는 경우,
  그리고 `자기소개 / 지원동기 / …` 형태의 복원 면접 흐름 목록(약 30건)은 분리하지 않고 한 항목으로 유지했다.
- **발표 제시문 요약**: 국가직 5분발표·상황형 과제의 `확인` 항목 일부는 질문문이 아니라 제시문 시나리오
  요약(예: "무인민원발급기 설치 여부 판단: …")이라 문장이 길고 서술형이다.
- **경계 사례 잔존**: 지역 상식 요약형 항목(예: "충남의 시·군 수(15개: …)")처럼 답이 본문에 포함된
  항목이 소수 남아 있다. 반대로 해설성 필터(어미·표지어 사전)가 태그 있는 요약형 항목
  (예: "…관련 질문", "…빈출")을 제외하므로 진짜 질문 소수가 누락될 수 있다.
- **중복**: 같은 질문이 공통 파일과 지역/직렬 파일에 모두 실린 경우 각각 별개 항목으로 유지된다
  (완전 동일 텍스트는 동일 파일 내에서만 중복 제거). AI 활용 시 임베딩 기반 중복 제거를 권장.
- **id 안정성**: `id`는 실행 시점의 정렬 순서 기반이라 원본 파일 추가·수정 시 재부여된다.

---

# 지역·직렬 컨텍스트 데이터셋 (AI 질문 생성용)

`data/03-regions/*.md`(지역)와 `data/04-jobs/*.md`(직렬)의 마크다운을, 질문 생성
프롬프트에 바로 주입할 "핵심 컨텍스트"로 뽑아낸 구조화 JSON.

- **생성 스크립트**: `scripts/build_context.py` (Python 표준 라이브러리만 사용)
- **재생성 방법**: 리포지토리 루트에서 `python3 scripts/build_context.py`
- **출력 파일**:
  - `data/07-structured/region_context.json` (지역 컨텍스트, 배열)
  - `data/07-structured/job_context.json` (직렬 컨텍스트, 배열)
  - 둘 다 UTF-8, `ensure_ascii=False`, `indent=2`
- 마크다운 원본은 읽기만 하며 수정하지 않는다. 원본 갱신 시 스크립트를 다시 실행한다.
- 배치 파일(경기도-주요시군·군단위-\*·광역시-자치구·중소시군-\*·특수채용·국가직 등)은
  헤딩(H1/H2)별로 개별 지역·직렬 엔트리로 분해한다. 파싱은 헤딩·불릿 기반 휴리스틱이다.

## `region_context.json` 스키마 (배열 요소)

| 필드 | 타입 | 설명 |
|---|---|---|
| `region` | string | 지역명. 단일 파일은 H1 제목(예: `서울특별시`), 배치 파일은 시·군·구 H2/H1 제목에서 번호·괄호 주석을 제거해 사용(예: `수원시`, `충북 음성군`, `영월군`) |
| `면접방식요약` | string | "면접 방식/특징" 섹션의 앞 1~2개 불릿 요약(≤320자). 없으면 `""`(배치 파일은 공통 면접 방식이 상위 도/광역 파일에만 있어 비는 경우가 많음) |
| `시정비전` | string | 도정/시정/군정 비전 또는 (없으면) 단체장 소개 줄. 최신 민선 9기(2026.7) 기조를 담음 |
| `슬로건` | string | 비전·단체장 줄의 인용부호(`"…"` `'…'` `「…」`) 안 슬로건. `슬로건` 키워드 뒤 인용구 우선. 없으면 `""` |
| `핵심사업` | string[] | 핵심 정책·사업·프로젝트 불릿의 토픽(굵은 라벨 우선, 없으면 첫 절). 최대 15개 |
| `지역현안` | string[] | 지역 현안 섹션 불릿 토픽. 결합 섹션(`비전·핵심 사업·지역 현안`)에서는 "현안/과제" 라벨 롤업 줄을 항목별로 분해. 최대 15개 |
| `source` | string | 원본 마크다운 파일명 |

## `job_context.json` 스키마 (배열 요소)

| 필드 | 타입 | 설명 |
|---|---|---|
| `job` | string | 직렬명. 단일 파일은 H1(예: `사회복지직`), 배치 파일은 직렬 H2(예: `교정직`, `운전직`, `우정직`)에서 번호·괄호 제거 |
| `개요` | string | 직렬 개요. "소속/특성/성격" 라벨 불릿 우선, 없으면 주요업무 앞 2개 요약(≤320자) |
| `주요업무` | string[] | "주요 업무" 섹션 불릿(단일 파일) 또는 개요 안 `업무:` 라벨 값 분해(배치 파일). 최대 15개 |
| `법령키워드` | string[] | "관련 법령·제도 키워드" 섹션의 `법령:` 항목, 또는 라벨 없는 항목 중 법/률/규칙/조례/령 등으로 끝나는 항목. 최대 60개 |
| `핵심제도` | string[] | 같은 섹션의 `제도·용어:`/`키워드:` 항목, 또는 법령이 아닌 짧은(≤40자) 제도·개념어. 최대 80개 |
| `source` | string | 원본 마크다운 파일명 |

## 생성 규모 (2026-07-06 기준)

- **지역 147개** (단일 지역 파일 17 + 배치 분해 130), **직렬 37개** (단일 15 + 배치 22).
- 지역 필드 채움률: 시정비전 141/147, 핵심사업 141/147, 슬로건 113/147, 지역현안 74/147, 면접방식요약 43/147.
- 직렬 필드 채움률: 핵심제도 37/37, 개요·주요업무 31/37, 법령키워드 30/37.

## 파서 한계

- **휴리스틱 파싱**: 헤딩 제목 키워드와 불릿 라벨에 의존한다. "완벽한 추출"이 아니라
  프롬프트에 넣을 핵심 컨텍스트를 안정적으로 뽑는 것이 목표다.
- **배치 파일의 `면접방식요약`**: 시·군·구별 세부 시트에는 개별 면접 방식이 거의 없고
  공통 방식이 상위 도/광역 파일에만 있어, 배치 엔트리의 `면접방식요약`은 대부분 비어 있다.
- **광역시-자치구(6개 광역시)**: 자치구 배치·현안 중심 파일이라 `시정비전`·`핵심사업`이 비고
  `지역현안`만 채워진다. 동일 지역명이 단독 파일(예: `부산광역시.md`)과 함께 **중복 등장**하므로
  소비 측에서 `(region, source)` 기준 병합·중복 제거를 권장한다.
- **`핵심사업` 노이즈**: `기타`·`5대 핵심 공약`·`민선 8기 성과…`처럼 라벨성/총론성 토픽이 일부 섞인다.
- **비전/슬로건이 헤딩에만 있는 경우**: 슬로건이 불릿이 아니라 H3 제목 괄호 안에만 있는 소수 파일은
  놓칠 수 있다(제목은 스캔하지 않음).
- **특수채용 6개 직렬**: `면접 방식/복원 기출/조직 현안·특화 키워드` 구조라 `직렬 개요`·`법령` 섹션이 없어
  `개요`·`주요업무`·`법령키워드`가 비고 `핵심제도`(특화 키워드)만 채워진다.
- **법령/제도 자동 분류**: 라벨이 없는 혼합 불릿은 어미 규칙으로 법령 vs 제도를 나누므로
  경계 항목에서 오분류가 소수 있을 수 있다.
